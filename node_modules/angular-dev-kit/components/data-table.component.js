"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var core_1 = require("@angular/core");
var ADKDataTable = (function () {
    function ADKDataTable() {
        this.data = [];
        this.columns = [];
    }
    return ADKDataTable;
}());
ADKDataTable.decorators = [
    { type: core_1.Component, args: [{
                selector: 'adk-dataTable',
                template: "<table *ngIf=\"data.length && columns.length\">\n                    <thead>\n                        <tr>\n                            <th *ngFor=\"let column of columns\">\n                                {{column.header}}\n                            </th>\n                        </tr>\n                    </thead>\n                    <tbody>\n                        <tr *ngFor=\"let item of data\">\n                            <td *ngFor=\"let column of columns\">\n                                <span *ngIf=\"column.name && !column.value\">{{item[column.name]}}</span>\n                                <span *ngIf=\"column.value && !column.name\">{{column.value(item)}}</span>\n                            </td>\n                        </tr>\n                    </tbody>\n                </table>\n                <span *ngIf=\"!data.length\">No Records Found!</span>\n                <span *ngIf=\"!columns.length\">No columns configured to display!</span>"
            },] },
];
/** @nocollapse */
ADKDataTable.ctorParameters = function () { return []; };
ADKDataTable.propDecorators = {
    'data': [{ type: core_1.Input },],
    'columns': [{ type: core_1.Input },],
};
exports.ADKDataTable = ADKDataTable;
//# sourceMappingURL=data-table.component.js.map