import { Formatable, Normalizable } from '../interfaces';
export declare class BaseModel implements Formatable, Normalizable {
    private metadata;
    constructor(initValue?: any);
    private defineMetadata(props);
    format(): any;
    normalize(from: any): any;
}
